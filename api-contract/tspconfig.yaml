emit:
  - "@typespec/openapi3"

options:
  "@typespec/openapi3":
    emitter-output-dir: "{project-root}"
    output-file: openapi.yaml
    file-type: yaml
    new-line: lf
    openapi-versions:
      - "3.1.0"

linter:
  extends:
    - "@typespec/http/all"
