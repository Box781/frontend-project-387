# Triage issue #2

Исходник: [время в календаре какое-то странное](https://github.com/Box781/frontend-project-387/issues/2)

## Как выглядела задача

Заголовок и текст были нарочно размытыми: «календарь врёт», «на три часа дня», «на телефоне и на компе по-разному», без шагов воспроизведения и без файлов.

Из этого нельзя было понять, чинить бэкенд, фронт или контракт.

## Что сделал агент

1. Комментарий `/oc explain this issue` — разбор без правок в коде.
2. Комментарий `/oc предложи план исправления` — постановка, PR не открывали.

## Насколько это помогло

| Было в issue | Стало после triage |
| --- | --- |
| «баг или я туплю» | Реальный баг: слоты в UTC, UI рисует локальное время браузера |
| «время какое-то не то» | Пример: 09:00 UTC у гостя в Москве выглядит как 12:00 |
| нет файлов | `backend/src/slots.ts`, `frontend/src/lib/dates.ts`, `GuestBookPage.tsx`, `AdminBookingsPage.tsx` |
| нет пути решения | Не трогать контракт и e2e; на фронте показывать UTC и подпись «Время (UTC)» |

Разбор полезный: причина, затронутые файлы и критерий готовности появились из пустого описания. `/oc fix` на этом шаге не запускали — нужна была постановка, а не сразу PR.
